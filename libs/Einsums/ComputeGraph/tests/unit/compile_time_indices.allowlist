# Files under libs/Einsums/ComputeGraph that may contain compile-time index code: none.
# Checked by Tests.Unit.Modules.ComputeGraph.CompileTimeIndices; the patterns and the rules are in
# cmake/scripts/check_compile_time_indices.cmake.
#
# ComputeGraph takes its contractions and permutations as string specs, which passes can rewrite.
# The templated Indices{...} API cannot be rewritten, and the module no longer uses it anywhere:
# library code, examples, docs and tests. With this list empty the check forbids it outright.
# Write new code with string specs; test values come from einsums::testing::reference_einsum and
# reference_permute. Do not add a line here.
#
# <count> <path relative to libs/Einsums/ComputeGraph>
