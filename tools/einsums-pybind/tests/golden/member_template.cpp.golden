// This file is generated by einsums-pybind. Do not edit.
//
// Source IR contains 1 class(es), 0 function(s), 0 enum(s).

#include <pybind11/pybind11.h>
#include <pybind11/complex.h>
#include <pybind11/numpy.h>
#include <pybind11/operators.h>
#include <pybind11/stl.h>

namespace py = pybind11;

PYBIND11_MODULE(fixture_member_template, m) {
    py::class_<einsums::fixture::Workspace>(m, "Workspace", R"einsums(Holder class with a member-template method. Each
INSTANTIATE_MEMBER_AS line on the method emits one Python binding
with the named template arg pinned to a concrete type.)einsums")
        .def(py::init<>())
        .def(
            "allocate",
            [](einsums::fixture::Workspace &self, int slot_id, std::string const &dtype) -> py::object {
                if (dtype == "float32" || dtype == "float" || dtype == "f4" || dtype == "f" || dtype == "single") {
                    return py::cast(
                        (self.*static_cast<float (einsums::fixture::Workspace::*)(int)>(&einsums::fixture::Workspace::allocate))(slot_id));
                }
                if (dtype == "float64" || dtype == "double" || dtype == "f8" || dtype == "d") {
                    return py::cast(
                        (self.*static_cast<double (einsums::fixture::Workspace::*)(int)>(&einsums::fixture::Workspace::allocate))(slot_id));
                }
                throw py::value_error("unsupported dtype: " + dtype);
            },
            py::arg("slot_id"), py::arg("dtype") = "float64", R"einsums(Allocate a slot keyed by an integer id, returning the new
slot's value. Two INSTANTIATE_MEMBER_AS lines share an argument
signature, differing only in the return type — same dtype-
dispatcher rules apply: collapse into one Python def taking
``dtype="..."``.)einsums");
}
