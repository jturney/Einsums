# module: fixture_member_template
# This file is auto-generated by einsums-pybind. Do not edit.
# pyright: reportInvalidTypeForm=false

from __future__ import annotations
from typing import Any, Literal, overload
from collections.abc import Callable
import enum
import numpy
import numpy.typing

# %%submodule: 
class Workspace:
    """Holder class with a member-template method. Each
    INSTANTIATE_MEMBER_AS line on the method emits one Python binding
    with the named template arg pinned to a concrete type.
    """
    def __init__(self) -> None: ...
    @overload
    def allocate(self, slot_id: int, dtype: Literal["float32", "float", "f4", "f", "single"]) -> float:
        """Allocate a slot keyed by an integer id, returning the new
        slot's value. Two INSTANTIATE_MEMBER_AS lines share an argument
        signature, differing only in the return type — same dtype-
        dispatcher rules apply: collapse into one Python def taking
        ``dtype="..."``.
        """
        ...
    @overload
    def allocate(self, slot_id: int, dtype: Literal["float64", "double", "f8", "d"] = "float64") -> float:
        """Allocate a slot keyed by an integer id, returning the new
        slot's value. Two INSTANTIATE_MEMBER_AS lines share an argument
        signature, differing only in the return type — same dtype-
        dispatcher rules apply: collapse into one Python def taking
        ``dtype="..."``.
        """
        ...
    @overload
    def allocate(self, slot_id: int, dtype: str = "float64") -> float:
        """Allocate a slot keyed by an integer id, returning the new
        slot's value. Two INSTANTIATE_MEMBER_AS lines share an argument
        signature, differing only in the return type — same dtype-
        dispatcher rules apply: collapse into one Python def taking
        ``dtype="..."``.
        """
        ...

