name: einsums-dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Development
  - cmake
  - ccache
  - cxx-compiler
  - ninja
  - lcov
  - pre-commit
  # Prebuilts to save einsums the work
  - catch2
  - fftw
  - fmt >=11,<11.1  # fmt 12 breaks spdlog; fmt 11.1+ malloc/free allocator fails on macOS libc++. Stay on 11.0.x
  - cpptrace
  - spdlog
  # HDF5
  # * zlib brings headers; hdf5 package only bring libzlib
  - hdf5
  - zlib
  - numpy
  - mimalloc
  # Python stuff
  - pybind11
  - pytest
  - numpy
  - pytest-cov
  - coverage
  - scipy
