name: einsums
channels: 
  - conda-forge
  - nodefaults
dependencies:
  - cmake
  - ccache
  - cxx-compiler
  - ninja
  # Prebuilts to save einsums the work
  - catch2
  - fftw
  - fmt >=11,<12  # fmt 12 has breaking API changes; spdlog (SPDLOG_FMT_EXTERNAL) needs fmt 11
  - cpptrace
  - spdlog
  # HDF5
  # * zlib brings headers; hdf5 package only bring libzlib
  - hdf5
  - zlib
  - numpy
  - mimalloc
  # Python stuff
  - pybind11
  - numpy
  - scipy
  - libblas[build=*openblas]
  - openblas[build=*openmp*]
  - fftw
