{
    "configurations": [
        {
            "cwd": "${projectDir}",
            "debugAdapter": "lldb-dap",
            "name": "Run (current target)",
            "program": "${buildDir}/bin/${target}"
        },
        {
            "args": [
                "--einsums:debug:no-attach-debugger"
            ],
            "cwd": "${projectDir}",
            "name": "Run Tests",
            "program": "${buildDir}/libs/Einsums/TensorAlgebra/tests/unit/Einsum1_test"
        },
        {
            "cwd": "${projectDir}",
            "debugAdapter": "lldb-dap",
            "name": "Debug (current target)",
            "program": "${buildDir}/bin/${target}"
        }
    ]
}
